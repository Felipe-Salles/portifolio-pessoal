---
phase: 05-security-hardening-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-deploy-headers.mjs
  - scripts/verify-csp-hash.mjs
  - scripts/generate-og-image.mjs
  - astro.config.mjs
  - vercel.json
  - package.json
  - package-lock.json
autonomous: true
requirements: [SEC-02, SEC-03, DEPLOY-02]

must_haves:
  truths:
    - "Running the live-header gate against a URL that lacks any of the six security headers fails with a named violation"
    - "vercel.json declares CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy and Permissions-Policy for every path"
    - "npm run verify fails if Nav.astro's inline script changes without its CSP hash being updated in vercel.json"
    - "npm run verify fails if any dist page gains an inline <style> tag or style attribute that style-src 'self' would block"
    - "astro build still produces dist/ so every Phase 1-4 gate keeps working, and mirrors the same bytes into .vercel/output/static/"
  artifacts:
    - path: "vercel.json"
      provides: "The six SEC-02/SEC-03 security headers applied to every path at the Vercel edge"
      contains: "Content-Security-Policy"
      min_lines: 20
    - path: "scripts/verify-csp-hash.mjs"
      provides: "Build-time CSP hash drift + header-shape gate over dist/ and vercel.json"
      contains: "CSPHASH SUMMARY"
      min_lines: 60
    - path: "scripts/verify-deploy-headers.mjs"
      provides: "DEPLOY-02 live production header gate (RED until plan 05-03 deploys)"
      contains: "DEPLOY SUMMARY"
      min_lines: 60
    - path: "astro.config.mjs"
      provides: "Vercel adapter wiring per CLAUDE.md"
      contains: "@astrojs/vercel"
  key_links:
    - from: "package.json"
      to: "scripts/verify-csp-hash.mjs"
      via: "verify:csp-hash appended to the composite verify chain"
      pattern: "verify:csp-hash"
    - from: "scripts/verify-csp-hash.mjs"
      to: "vercel.json"
      via: "reads the Content-Security-Policy value and compares it to hashes recomputed from dist/"
      pattern: "vercel\\.json"
    - from: "astro.config.mjs"
      to: "@astrojs/vercel"
      via: "adapter key with staticHeaders enabled"
      pattern: "adapter:\\s*vercel\\("
---

## Phase Goal

**As a** visitor (recruiter, client or peer) landing on Felipe's portfolio, **I want to** reach the site at a public HTTPS address whose browser-enforced security headers block framing, script injection and protocol downgrade, **so that** the site itself is the proof that its author takes technical security seriously.

This plan builds the *contract half* of that slice: the failing end-to-end test that defines "correct headers", the `vercel.json` that will satisfy it, and the local gate that keeps the CSP honest. The slice goes live in plan 05-03.

<objective>
Author the DEPLOY-02 live-header gate first (RED — no deployment exists yet), then install `@astrojs/vercel`, hand-author the six security headers in a root `vercel.json`, and add the build-time CSP-hash drift gate that makes a hand-written hash safe to ship.

Purpose: SEC-02/SEC-03 are HTTP response headers on a 100% static site; per 05-RESEARCH.md Pitfall 1 the adapter's `staticHeaders` bridge has a documented history of silently not delivering headers for `output: "static"`, so `vercel.json` is the deterministic delivery mechanism and the adapter is installed for CLAUDE.md compliance and the Vercel build target.
Output: two new gate scripts, one new root config file, an adapter-wired `astro.config.mjs`, and two new npm script entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-hardening-deploy/05-CONTEXT.md
@.planning/phases/05-security-hardening-deploy/05-RESEARCH.md
@.planning/phases/05-security-hardening-deploy/05-PATTERNS.md

<interfaces>
<!-- The gate-script contract every verify-*.mjs in this repo implements. Follow it exactly. -->

1. `#!/usr/bin/env node` shebang, then a block comment naming the requirement IDs gated,
   the zero-third-party-dependency policy, and the literal summary-line format.
2. A hard precondition guard that prints a fixed message and exits 1 (missing dist/,
   missing config file, missing CLI arg).
3. Accumulate every finding into a `violations` array — never exit on the first failure.
4. Print each violation to stderr with a consistent per-line prefix.
5. Print exactly ONE fixed-format summary line to stdout as the last line before exit.
6. `process.exit(0)` only when violations is empty.

Verified facts about the current build (measured against dist/ during planning, 2026-09-04):

  dist/index.html            1 inline <script>, 0 <style> tags, 0 style="" attributes
  dist/404.html              1 inline <script>, 0 <style> tags, 0 style="" attributes
  dist/og-template/index.html 0 inline <script>, 0 <style> tags, 0 style="" attributes

  Both inline scripts are byte-identical. Their SHA-256, base64-encoded, was
  sha256-9rao6+7BIzvFHjbooNK6nWAJiJNi1ORIwyjtuaVZ5Yo=
  This value MUST be recomputed from a fresh build at execution time (05-RESEARCH.md
  Assumption A3) — it is recorded here only so a mismatch is recognizable, never to be
  copied blindly.

Adapter build-output behavior (verified against @astrojs/vercel source during planning):
  The adapter does NOT redirect outDir. `astro build` still writes dist/, and an
  `astro:build:done` hook copies dist/ into .vercel/output/static/. Every existing
  Phase 1-4 gate that reads dist/ therefore keeps working unchanged. The copy happens
  BEFORE the `build` script's second command (`node scripts/generate-og-image.mjs`)
  runs — see Task 2's parity requirement.

Current package.json verify chain (append-only, never reorder):
  verify = npm run build && verify:sec01 && verify:tokens && verify:shell
           && verify:sections && verify:schema && verify:seo && verify:a11y

Existing npm scripts: dev, build, preview, check, generate:favicons, generate:og,
verify:sec01, verify:tokens, verify:shell, verify:sections, verify:schema,
verify:seo, verify:a11y, verify.

Current astro.config.mjs shape (top-level keys, in order):
  output: "static"
  site: "https://portfolio-felipe-salles.example"   <- TODO(Phase 5), swapped in plan 05-03, NOT here
  integrations: [ icon(), sitemap({ filter }) ]
  vite: { plugins: [tailwindcss()] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing DEPLOY-02 live-header gate</name>
  <files>scripts/verify-deploy-headers.mjs, package.json</files>
  <read_first>
    - scripts/verify-no-external-origins.mjs (full file — the canonical guard / violations-array / summary-line / exit-code contract this repo uses)
    - scripts/verify-seo.mjs lines 40-51 and its final 30 lines (the "single named binding for the origin, never a duplicated literal" discipline and the multi-group ok/fail summary style)
    - .planning/phases/05-security-hardening-deploy/05-PATTERNS.md section "scripts/verify-deploy-headers.mjs" (including the explicit divergence note: guard on the missing argv, NOT on dist/)
    - .planning/phases/05-security-hardening-deploy/05-RESEARCH.md "Pattern 3" and "Common Pitfalls" #2
    - package.json (the scripts block — this task adds one standalone entry, it does NOT touch the composite verify chain)
  </read_first>
  <action>
    Create `scripts/verify-deploy-headers.mjs` as plain Node ESM using only Node builtins and the global `fetch` (Node >= 22.12 per package.json engines). No npm dependency, matching every existing gate.

    Take the production URL from `process.argv[2]` into a single named binding and reference only that binding everywhere — never write a second copy of any domain literal in this file. If argv[2] is absent, print exactly `usage: node scripts/verify-deploy-headers.mjs <production-url>` to stderr and exit 1. If the URL's protocol is not `https:`, add a violation named `https` (DEPLOY-01 requires HTTPS) and exit 1 without fetching.

    Fetch two paths off that origin so the gate proves the `/(.*)` header rule matches sub-paths and not just the root: the origin itself (`/`) and a deliberately non-existent path (`/__gsd-404-probe`, which must render the custom 404). Use `fetch(url, { method: "GET", redirect: "follow" })`. Record each response's status. Require status 200 for `/`; for the 404 probe require status 404. Apply the full header assertion set below to BOTH responses independently.

    Header assertions (each its own named check, each contributing to the violations array):
    - `content-security-policy` present and its value contains all of: `default-src 'self'`, `script-src`, `style-src 'self'`, `img-src 'self'`, `font-src 'self'`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`; and its `script-src` directive contains both `'self'` and at least one `'sha256-` token.
    - `strict-transport-security` present, with a `max-age` of at least 31536000, containing `includeSubDomains`, and NOT containing `preload` (D-03 locks preload out — its presence is a violation, not a pass).
    - `x-frame-options` present and exactly `DENY` (case-insensitive compare on the value).
    - `x-content-type-options` present and exactly `nosniff`.
    - `referrer-policy` present and exactly `strict-origin-when-cross-origin`.
    - `permissions-policy` present and containing all of `camera=()`, `microphone=()`, `geolocation=()`.

    Add one drift check between the live policy and the repo: if `vercel.json` exists in the working directory, extract its `Content-Security-Policy` value, collect every `'sha256-...'` token from both the live CSP and the vercel.json CSP, and add a violation named `hash-drift` if the two sets are not identical. Skip this check silently when `vercel.json` is absent so the script stays runnable from anywhere.

    Print every header's actual value to stdout as `<header-name>: <value or (missing)>` per fetched path, then each violation to stderr as `verify-deploy-headers: <check> — <detail>`, then exactly one summary line to stdout in the form `DEPLOY SUMMARY url=<url> paths=<n> root_status=<n> notfound_status=<n> csp=<ok|fail> hsts=<ok|fail> xfo=<ok|fail> xcto=<ok|fail> refpol=<ok|fail> permpol=<ok|fail> violations=<n>` and exit 0 only when violations is empty.

    Then add `"verify:deploy": "node scripts/verify-deploy-headers.mjs"` to package.json scripts, placed after `verify:a11y`. Do NOT append it to the composite `verify` chain — it needs a live URL argument and can only pass after plan 05-03 deploys (05-PATTERNS.md "Divergence from every existing verify-*.mjs").
  </action>
  <acceptance_criteria>
    - `node scripts/verify-deploy-headers.mjs` with no argument exits 1 and stderr contains the exact string `usage: node scripts/verify-deploy-headers.mjs <production-url>`
    - `node scripts/verify-deploy-headers.mjs http://example.com` exits 1 and names the `https` check on stderr
    - `node scripts/verify-deploy-headers.mjs https://example.com` exits 1 and names at least the `csp`, `hsts`, `xfo` and `permpol` checks on stderr — this is the RED state proving the gate detects a real site that lacks the header set
    - stdout of the `https://example.com` run contains a line matching `^DEPLOY SUMMARY url=https://example.com ` and ending in a `violations=` count greater than 0
    - Every import specifier in `scripts/verify-deploy-headers.mjs` starts with `node:` (zero third-party dependencies)
    - `grep -c 'vercel.app' scripts/verify-deploy-headers.mjs` returns 0 — no domain literal is duplicated into the script
    - `grep -q '"verify:deploy"' package.json` succeeds AND the composite `verify` script string does NOT contain `verify:deploy`
  </acceptance_criteria>
  <verify>
    <automated>! node scripts/verify-deploy-headers.mjs 2>&1 | grep -q "usage: node scripts/verify-deploy-headers.mjs" ; node scripts/verify-deploy-headers.mjs 2>&1 | grep -q "usage:" && ! node scripts/verify-deploy-headers.mjs https://example.com && node -e "const p=require('./package.json');if(!p.scripts['verify:deploy'])throw new Error('verify:deploy missing');if(p.scripts.verify.includes('verify:deploy'))throw new Error('verify:deploy must not be in the composite chain');console.log('OK')"</automated>
  </verify>
  <done>A committed, dependency-free gate defines exactly what "correct production headers" means, proves it can fail against a real HTTPS site that lacks them, and is wired as a standalone npm script that plan 05-03 will turn green.</done>
</task>

<task type="auto">
  <name>Task 2: Install the Vercel adapter and author the six security headers</name>
  <files>astro.config.mjs, vercel.json, package.json, package-lock.json, scripts/generate-og-image.mjs</files>
  <read_first>
    - astro.config.mjs (full file — the current import block, the TODO(Phase 5) site placeholder that this task must leave untouched, and the top-level key order)
    - package.json (dependencies block — integrations live in dependencies, tooling in devDependencies)
    - .gitignore (confirms `.vercel/` and `dist/` are ignored; `vercel.json` must be tracked)
    - scripts/generate-og-image.mjs (full file — its dist/ and public/ write paths, needed for the .vercel/output/static parity requirement below)
    - .planning/phases/05-security-hardening-deploy/05-RESEARCH.md "Architecture Patterns > Pattern 1" and "Common Pitfalls" #1 and #3
    - .planning/phases/05-security-hardening-deploy/05-PATTERNS.md sections "astro.config.mjs (modified — additive)" and "vercel.json (new)"
    - .planning/phases/05-security-hardening-deploy/05-CONTEXT.md (D-03 no preload, D-04 enforcing CSP not Report-Only)
    - src/components/Nav.astro lines 126-140 (the one inline script whose hash the CSP must cover)
  </read_first>
  <action>
    Install the adapter with `npm install @astrojs/vercel@^11.0.10` — it goes in `dependencies`, alongside `@astrojs/sitemap` and `astro-icon`, not devDependencies. 05-RESEARCH.md's Package Legitimacy Audit records this package as slopcheck `[OK]`, first-party `withastro`, no postinstall script, disposition Approved — no legitimacy checkpoint is required for it.

    Edit `astro.config.mjs`: add `import vercel from "@astrojs/vercel";` to the existing import block, same default-import style as the `@astrojs/sitemap` import. Add a top-level `adapter: vercel({ staticHeaders: true })` key as a sibling of `output`/`site`/`integrations`/`vite` (not inside the integrations array), placed after `site`. Keep `output: "static"`. Do NOT add a `security.csp` key — per D-04 and 05-RESEARCH.md Pitfall 1 the enforcing CSP is delivered by `vercel.json`, and enabling Astro's CSP feature would additionally emit a `<meta>` tag that cannot express `frame-ancestors` anyway. Leave the `site:` value and its `TODO(Phase 5)` comment exactly as they are — plan 05-03 owns that swap.

    Run `npm run build`, then recompute the SHA-256 of every inline `<script>` (a `<script>` tag with no `src=` attribute) across every `.html` file under `dist/`, base64-encoded, prefixed `sha256-`. Use that freshly computed value — do NOT copy the value recorded in this plan's interfaces block without confirming it matches.

    Create `vercel.json` at the repo root, 2-space indent, double-quoted keys, no trailing commas (matching tsconfig.json's formatting). Top-level keys: `"$schema"` set to `https://openapi.vercel.sh/vercel.json`, and `"headers"` as an array with exactly one entry whose `"source"` is `"/(.*)"` and whose `"headers"` array holds these six key/value objects, with these exact values:
    - `Content-Security-Policy` → `default-src 'self'; script-src 'self' '<COMPUTED_HASH>'; style-src 'self'; img-src 'self' data:; font-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'` where `<COMPUTED_HASH>` is the recomputed `sha256-...` value (include one quoted hash token per distinct inline script found).
    - `Strict-Transport-Security` → `max-age=63072000; includeSubDomains` — no `preload` token, per D-03.
    - `X-Frame-Options` → `DENY`
    - `X-Content-Type-Options` → `nosniff`
    - `Referrer-Policy` → `strict-origin-when-cross-origin`
    - `Permissions-Policy` → `camera=(), microphone=(), geolocation=(), interest-cohort=()`

    Then run `npm run build` again and assert build-output parity. The adapter copies `dist/` into `.vercel/output/static/` during `astro:build:done`, which runs before the `build` script's second command (`node scripts/generate-og-image.mjs`). Confirm `dist/index.html` still exists (every Phase 1-4 gate depends on it) and that `.vercel/output/static/index.html`, `.vercel/output/static/404.html` and `.vercel/output/static/og-image.png` exist and are byte-identical to their `dist/` counterparts. If `og-image.png` is missing or differs, extend `scripts/generate-og-image.mjs` so that whenever the directory `.vercel/output/static/` exists it also writes the same PNG bytes to `.vercel/output/static/og-image.png` — a deployed copy that is one build stale is a real DEPLOY-01 defect, not a cosmetic one. Keep the existing `OG-IMAGE SUMMARY` line format working; extend its `outputs=` list if a third destination is written.

    Finally run `npm run verify` and confirm all seven existing gates still exit 0 — adding the adapter must not regress SEC-01, tokens, shell, sections, schema, SEO or a11y.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');if(!p.dependencies['@astrojs/vercel'])throw 0"` succeeds and `@astrojs/vercel` is absent from `devDependencies`
    - `grep -q 'adapter: vercel(' astro.config.mjs` succeeds and `grep -q 'staticHeaders: true' astro.config.mjs` succeeds
    - `grep -q 'security' astro.config.mjs` returns no match — Astro's CSP feature is deliberately not enabled
    - `astro.config.mjs` still contains the literal `https://portfolio-felipe-salles.example` and its `TODO(Phase 5)` comment (untouched by this plan)
    - `vercel.json` parses as JSON, `headers` has exactly one entry, its `source` is `/(.*)`, and its `headers` array contains exactly the six keys `Content-Security-Policy`, `Strict-Transport-Security`, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`
    - The `Strict-Transport-Security` value contains `includeSubDomains` and does NOT contain `preload`
    - Every `sha256-` token appearing in the `Content-Security-Policy` value is reproduced exactly by hashing an inline `<script>` body found in `dist/`, and every inline script body found in `dist/` produces a hash that appears in that value
    - After `npm run build`: `dist/index.html` exists, `.vercel/output/static/index.html` exists, and `.vercel/output/static/og-image.png` is byte-identical to `dist/og-image.png`
    - `npm run verify` exits 0 with all seven pre-existing gate summary lines reporting zero violations
    - `git check-ignore vercel.json` exits nonzero (the file is tracked) while `git check-ignore .vercel` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node -e "
const fs=require('fs'),cp=require('crypto'),path=require('path');
const c=JSON.parse(fs.readFileSync('vercel.json','utf8'));
if(c.headers.length!==1||c.headers[0].source!=='/(.*)')throw new Error('bad headers entry');
const m=Object.fromEntries(c.headers[0].headers.map(h=>[h.key,h.value]));
for(const k of ['Content-Security-Policy','Strict-Transport-Security','X-Frame-Options','X-Content-Type-Options','Referrer-Policy','Permissions-Policy'])if(!m[k])throw new Error('missing '+k);
if(!/includeSubDomains/.test(m['Strict-Transport-Security'])||/preload/.test(m['Strict-Transport-Security']))throw new Error('HSTS violates D-03');
if(m['X-Frame-Options']!=='DENY'||m['X-Content-Type-Options']!=='nosniff'||m['Referrer-Policy']!=='strict-origin-when-cross-origin')throw new Error('bad SEC-03 value');
function walk(d,a=[]){for(const e of fs.readdirSync(d)){const p=path.join(d,e);fs.statSync(p).isDirectory()?walk(p,a):p.endsWith('.html')&&a.push(p)}return a}
const found=new Set();
for(const f of walk('dist'))for(const g of fs.readFileSync(f,'utf8').matchAll(/<script(?![^>]*\ssrc=)[^>]*>([\s\S]*?)<\/script>/g))found.add('sha256-'+cp.createHash('sha256').update(g[1],'utf8').digest('base64'));
const declared=new Set([...m['Content-Security-Policy'].matchAll(/'(sha256-[A-Za-z0-9+/=]+)'/g)].map(x=>x[1]));
for(const h of found)if(!declared.has(h))throw new Error('dist hash not in vercel.json: '+h);
for(const h of declared)if(!found.has(h))throw new Error('stale hash in vercel.json: '+h);
if(!fs.existsSync('dist/index.html'))throw new Error('dist/ no longer produced');
if(!fs.existsSync('.vercel/output/static/index.html'))throw new Error('adapter did not mirror to .vercel/output/static');
if(Buffer.compare(fs.readFileSync('dist/og-image.png'),fs.readFileSync('.vercel/output/static/og-image.png'))!==0)throw new Error('og-image.png drift between dist and .vercel/output/static');
console.log('OK')" && npm run verify</automated>
  </verify>
  <done>The repo declares all six SEC-02/SEC-03 headers in a platform-native config file with a CSP hash provably derived from the real build, the Vercel adapter is installed per CLAUDE.md without displacing dist/, the deployed asset tree matches dist/ byte for byte, and no Phase 1-4 gate regressed.</done>
</task>

<task type="auto">
  <name>Task 3: Add the CSP-hash drift gate and wire it into npm run verify</name>
  <files>scripts/verify-csp-hash.mjs, package.json</files>
  <read_first>
    - scripts/verify-no-external-origins.mjs (full file — copy its `fail()` guard shape, violations array, summary line and exit-code contract verbatim in structure)
    - vercel.json (the file this gate reads — created in Task 2)
    - package.json (the scripts block — append-only wiring, never reorder existing gates)
    - .planning/phases/05-security-hardening-deploy/05-PATTERNS.md sections "scripts/verify-csp-hash.mjs" and "package.json verify-chain wiring"
    - .planning/phases/05-security-hardening-deploy/05-RESEARCH.md "Pattern 2" and "Anti-Patterns to Avoid" (the hardcoded-hash-without-drift-check anti-pattern this gate exists to prevent)
  </read_first>
  <action>
    Create `scripts/verify-csp-hash.mjs` as plain Node ESM importing only from `node:fs`, `node:path` and `node:crypto`. Open with the repo's standard shebang plus a contract comment naming SEC-02 and SEC-03 and stating the exact summary-line format.

    Guards: if `dist/` is absent, print `dist/ not found — run npm run build first` and exit 1. If `vercel.json` is absent, print `vercel.json not found — SEC-02's CSP header source is missing` and exit 1.

    Walk `dist/` recursively collecting every `.html` file. For each, extract every inline `<script>` (a `<script>` tag carrying no `src=` attribute) using the pattern `<script(?![^>]*\ssrc=)[^>]*>([\s\S]*?)</script>`, hash each captured body as SHA-256 base64 prefixed `sha256-`, and collect the deduplicated set. Also count every `<style` tag occurrence and every ` style="` attribute occurrence across those files.

    Read `vercel.json`, locate the headers entry whose `source` is `/(.*)`, and pull its `Content-Security-Policy` value plus the other five header values.

    Record these violations:
    - `missing-hash`: an inline-script hash computed from dist/ that does not appear as a quoted token inside the CSP's `script-src` directive.
    - `stale-hash`: a `'sha256-...'` token in `script-src` that no dist/ inline script reproduces.
    - `inline-style`: any `<style` tag or ` style="` attribute found in dist/ — `style-src 'self'` would block it, so its appearance means the policy and the build have diverged.
    - `csp-directive`: the CSP value missing any of `default-src 'self'`, `style-src 'self'`, `img-src 'self'`, `font-src 'self'`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, or a `script-src` that omits `'self'`.
    - `sec03-header`: `Strict-Transport-Security` missing, or with a `max-age` below 31536000, or missing `includeSubDomains`, or containing `preload` (D-03 forbids it); `X-Frame-Options` not exactly `DENY`; `X-Content-Type-Options` not exactly `nosniff`; `Referrer-Policy` not exactly `strict-origin-when-cross-origin`; `Permissions-Policy` missing any of `camera=()`, `microphone=()`, `geolocation=()`.

    Print each violation to stderr as `verify-csp-hash: <check> — <detail>`, then exactly one summary line to stdout: `CSPHASH SUMMARY pages=<n> inline_scripts=<n> hashes=<n> matched=<n> stale=<n> inline_styles=<n> sec03=<ok|fail> violations=<n>`. Exit 0 only when violations is empty.

    Then wire it in package.json: add `"verify:csp-hash": "node scripts/verify-csp-hash.mjs"` after the `verify:deploy` entry, and append ` && npm run verify:csp-hash` to the END of the composite `verify` chain, preserving the existing `npm run build && npm run verify:sec01 && ... && npm run verify:a11y` prefix byte for byte. Do not reorder, rename or remove any existing entry.

    Prove the gate actually fails by running each negative mutation below against a fresh build, restoring the file afterwards.
  </action>
  <acceptance_criteria>
    - `npm run verify` exits 0 and its output contains a line matching `^CSPHASH SUMMARY pages=[0-9]+ inline_scripts=[0-9]+ hashes=[0-9]+ matched=[0-9]+ stale=0 inline_styles=0 sec03=ok violations=0$`
    - Every import specifier in `scripts/verify-csp-hash.mjs` starts with `node:`
    - Negative proof — each mutation must make `node scripts/verify-csp-hash.mjs` exit nonzero AND name the corresponding check on stderr, with `vercel.json` restored from git afterwards: (a) flip one character inside the `sha256-` token in `vercel.json` → `missing-hash` and `stale-hash`; (b) delete `frame-ancestors 'none';` from the CSP value → `csp-directive`; (c) append `; preload` to the `Strict-Transport-Security` value → `sec03-header`; (d) change `X-Frame-Options` to `SAMEORIGIN` → `sec03-header`
    - Negative proof for the inline-style check: appending `<style>body{}</style>` to `dist/index.html` makes the gate exit nonzero naming `inline-style`; `npm run build` restores it
    - With `dist/` temporarily renamed away, the gate exits 1 printing `dist/ not found — run npm run build first`; restore `dist/` afterwards
    - The composite `verify` script string contains all nine of `verify:sec01`, `verify:tokens`, `verify:shell`, `verify:sections`, `verify:schema`, `verify:seo`, `verify:a11y`, `verify:csp-hash` in that relative order, and still does NOT contain `verify:deploy`
  </acceptance_criteria>
  <verify>
    <automated>npm run verify && npm run verify:csp-hash | grep -Eq "^CSPHASH SUMMARY pages=[0-9]+ inline_scripts=[0-9]+ hashes=[0-9]+ matched=[0-9]+ stale=0 inline_styles=0 sec03=ok violations=0$" && node -e "const fs=require('fs');const s=fs.readFileSync('vercel.json','utf8');fs.writeFileSync('vercel.json.bak',s);fs.writeFileSync('vercel.json',s.replace(/'sha256-./,\"'sha256-X\"))" && ! node scripts/verify-csp-hash.mjs; node -e "const fs=require('fs');fs.renameSync('vercel.json.bak','vercel.json')" && node scripts/verify-csp-hash.mjs && node -e "const p=require('./package.json');const v=p.scripts.verify;for(const s of ['verify:sec01','verify:tokens','verify:shell','verify:sections','verify:schema','verify:seo','verify:a11y','verify:csp-hash'])if(!v.includes(s))throw new Error('chain missing '+s);if(v.includes('verify:deploy'))throw new Error('verify:deploy leaked into the chain');console.log('OK')"</automated>
  </verify>
  <done>A hand-authored CSP hash is now safe to ship: any change to Nav.astro's inline script, any stale hash, any new inline style, any dropped CSP directive and any weakened SEC-03 header value breaks `npm run verify` at build time instead of silently breaking the live site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → Vercel edge | The only place SEC-02/SEC-03 are actually enforced; everything in this repo is just a declaration until the edge sends it |
| `vercel.json` → deployed response | A hand-authored policy can drift from the build it is supposed to describe; the drift is invisible without a gate |
| npm registry → build environment | One new dependency enters the build with full filesystem/network access during CI |
| Gate script → developer confidence | A gate that passes vacuously converts an unverified claim into a believed one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Hardcoded CSP `script-src` hash going stale when `Nav.astro` changes, blocking the mobile nav in production | mitigate | `scripts/verify-csp-hash.mjs` recomputes every inline-script hash from `dist/` on every `npm run verify` and fails on both missing and stale hashes; four negative mutations in Task 3 prove it can fail |
| T-05-01-02 | Spoofing | Clickjacking via a third-party frame | mitigate | `frame-ancestors 'none'` in the CSP plus `X-Frame-Options: DENY` in `vercel.json`, both asserted locally by Task 3 and live by Task 1's gate |
| T-05-01-03 | Information Disclosure | Full-URL referrer leaking to external project/social link targets | mitigate | `Referrer-Policy: strict-origin-when-cross-origin` declared in `vercel.json` and asserted by both gates |
| T-05-01-04 | Elevation of Privilege | A future injected script reaching camera/microphone/geolocation APIs | mitigate | `Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()` declared and asserted by both gates |
| T-05-01-05 | Tampering | Protocol downgrade / MITM on a direct HTTP link after first visit | mitigate | `Strict-Transport-Security: max-age=63072000; includeSubDomains` (no `preload`, per D-03 — Task 3 treats a `preload` token as a violation so the decision cannot silently drift) |
| T-05-01-06 | Tampering | Adding the adapter silently relocating build output and vacuously passing every Phase 1-4 `dist/`-based gate | mitigate | Task 2 asserts `dist/index.html` still exists, asserts `.vercel/output/static/` parity including `og-image.png` bytes, and re-runs the full seven-gate `npm run verify` |
| T-05-01-SC | Tampering | npm install of `@astrojs/vercel` | accept | 05-RESEARCH.md Package Legitimacy Audit records slopcheck `[OK]`, first-party `withastro` monorepo, published 2022-04-04, no `postinstall` script — disposition Approved, so no blocking legitimacy checkpoint is required |
</threat_model>

<verification>
1. `node scripts/verify-deploy-headers.mjs https://example.com` exits nonzero and names the missing headers — the RED state for DEPLOY-02.
2. `vercel.json` declares all six headers with the exact D-03/D-04-compliant values.
3. `npm run build` still writes `dist/` and mirrors byte-identical output into `.vercel/output/static/`.
4. `npm run verify` runs eight gates and exits 0, including `CSPHASH SUMMARY ... violations=0`.
5. Six negative mutations each break `verify:csp-hash` and name their check.
</verification>

<success_criteria>
- SEC-02: an enforcing (not Report-Only, per D-04) CSP with `default-src 'self'` and a build-derived `script-src` hash is declared in `vercel.json` and gated locally.
- SEC-03: HSTS (no preload, per D-03), X-Frame-Options, X-Content-Type-Options, Referrer-Policy and Permissions-Policy are declared in `vercel.json` and gated locally.
- DEPLOY-02: the live-URL header gate exists, is dependency-free, is proven able to fail, and is wired as a standalone `verify:deploy` script outside the build-time chain.
- `@astrojs/vercel` is installed with `staticHeaders: true` per CLAUDE.md without displacing `dist/` or regressing any Phase 1-4 gate.
</success_criteria>

<output>
Create `.planning/phases/05-security-hardening-deploy/05-01-SUMMARY.md` when done
</output>
