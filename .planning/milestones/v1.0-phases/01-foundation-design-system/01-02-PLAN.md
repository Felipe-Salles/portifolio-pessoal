---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/global.css
  - src/pages/index.astro
  - scripts/verify-design-tokens.mjs
autonomous: true
requirements: [SEC-01]
requirements_note: "SEC-01 applies here because src/styles/global.css is one of the three SEC-01 surfaces named in 01-PATTERNS.md — an @import url() to an external host in this file would breach it. This plan's primary gate is ROADMAP Phase 1 success criterion #2 (DESIGN.md tokens wired into @theme and usable by any component), which carries no requirement ID of its own; LAY-02, the requirement these tokens ultimately serve, is verified in Phase 3 per REQUIREMENTS.md traceability."
mvp_mode: true

must_haves:
  truths:
    - "Any component can write `bg-surface-container`, `text-display-lg`, `font-mono-label`, `px-margin-mobile`, or `rounded-lg` and get the exact DESIGN.md value"
    - "All 47 DESIGN.md colour tokens are present as CSS custom properties in the built stylesheet"
    - "The 7 named type roles carry their size, line-height, letter-spacing, and weight together — not size alone"
    - "The glassmorphism, background-grid, atmospheric-glow, and cyan-glow-stroke visual primitives from the prototype render"
    - "Token drift from DESIGN.md is caught automatically, not by eye"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Tailwind v4 @theme block with every DESIGN.md token, plus the 6 prototype custom CSS classes"
      min_lines: 120
      contains: "@theme"
    - path: "scripts/verify-design-tokens.mjs"
      provides: "Automated DESIGN.md-to-@theme fidelity gate (parses DESIGN.md as source of truth)"
      min_lines: 80
    - path: "src/pages/index.astro"
      provides: "Token gallery exercising a representative utility from each token family"
  key_links:
    - from: "src/styles/global.css"
      to: "Arquivos de design/DESIGN.md frontmatter"
      via: "@theme custom properties mirroring every colors/typography/rounded/spacing key"
      pattern: "--color-primary-container:\\s*#00f0ff"
    - from: "scripts/verify-design-tokens.mjs"
      to: "Arquivos de design/DESIGN.md"
      via: "runtime parse of the YAML frontmatter as the expected-value source"
      pattern: "DESIGN\\.md"
    - from: "src/pages/index.astro"
      to: "src/styles/global.css"
      via: "Tailwind utility classes generated from @theme tokens"
      pattern: "text-display-lg|bg-surface-container"
---

<objective>
Translate the entire approved `DESIGN.md` token set into Tailwind v4's CSS-first `@theme` block, port the six custom CSS classes the prototype needs that no utility can express, and lock the translation behind a script that re-derives the expected values from `DESIGN.md` itself.

Purpose: This is the contract every component in Phases 2–5 depends on. A single wrong hex or a missing line-height modifier here silently propagates into every section built later, and LAY-02 (full visual fidelity, verified in Phase 3) becomes unachievable without rework. 47 colour values cannot be checked reliably by eye, so the fidelity gate is automated and derives its expectations from the source document rather than hardcoding a second copy.
Output: A complete `@theme` block, working glassmorphism/grid/glow primitives, and a passing `npm run verify:tokens`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-PATTERNS.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
@Arquivos de design/DESIGN.md
@Arquivos de design/code.html
</context>

<constraints>
**Hard scope boundary (locked by `01-CONTEXT.md`):** Do NOT build a nav bar, footer, hero, dossier, tech stack, projects grid, or contact section. `src/pages/index.astro` stays scaffolding — in this plan it becomes a token gallery whose only job is to force Tailwind to emit utilities so they can be asserted against. Phase 2 replaces it.

**Do NOT create** a `tailwind.config.js` / `.mjs` / `.ts`. All tokens are CSS custom properties inside a single `@theme { }` block.

**Do NOT** copy `code.html`'s `fontSize: ["32px", { ... }]` JS tuple syntax into CSS. That is Tailwind v3 JS-config syntax and has no meaning inside `@theme` (RESEARCH.md Pitfall 4).

**Do NOT** add any `@import url(...)` pointing at an external host to `global.css` — `npm run verify:sec01` must keep passing.
</constraints>

<source_of_truth>
`Arquivos de design/DESIGN.md` frontmatter is canonical. `code.html`'s `tailwind.config` block agrees with it key-for-key and value-for-value for **colors, typography, and spacing** — but the two sources genuinely diverge on **radius**, and `code.html` is wrong there:

| DESIGN.md `rounded:` (canonical) | `code.html` `borderRadius:` (do not copy) |
|---|---|
| `sm: 0.125rem`, `DEFAULT: 0.25rem`, `md: 0.375rem`, `lg: 0.5rem`, `xl: 0.75rem`, `full: 9999px` | `DEFAULT: 0.125rem`, `lg: 0.25rem`, `xl: 0.5rem`, `full: 0.75rem` |

`code.html`'s `full: 0.75rem` is a prototype authoring error — 0.75rem is not a pill radius, and the prototype itself uses `rounded-full` for circular status dots and 48px contact icon circles. Use DESIGN.md's six keys.
</source_of_truth>

<tasks>

<task type="auto">
  <name>Task 1: Wire every DESIGN.md token into the Tailwind v4 @theme block</name>

  <files>src/styles/global.css, src/pages/index.astro</files>

  <read_first>
    - `Arquivos de design/DESIGN.md` lines 1-104 — the canonical frontmatter: 47 colour keys, 7 typography roles, 6 `rounded` keys, 5 `spacing` keys
    - `.planning/phases/01-foundation-design-system/01-PATTERNS.md` — the `src/styles/global.css` section, which lists all 47 colour key/value pairs explicitly and confirms `code.html` matches DESIGN.md for colours/typography/spacing but not radius
    - `.planning/phases/01-foundation-design-system/01-RESEARCH.md` — Architecture Pattern 1 (the verified CSS-first `@theme` syntax) and Pitfall 4 (the v3-tuple trap)
    - `.planning/phases/01-foundation-design-system/01-UI-SPEC.md` — "Implementation Notes (Tailwind v4 `@theme` mapping)" and the Spacing/Typography/Color tables
    - `src/styles/global.css` — current state (contains only the Tailwind import from plan 01-01)
    - `src/pages/index.astro` — current state from plan 01-01 Task 3
  </read_first>

  <action>
Extend `src/styles/global.css` with a single `@theme { }` block placed after the existing `@import "tailwindcss";`. Populate it from `Arquivos de design/DESIGN.md`'s frontmatter using Tailwind v4's CSS-first namespaces.

**Colours** — one `--color-{key}: {hex};` per DESIGN.md `colors:` entry, all 47 of them, with keys kept dash-cased exactly as written (`--color-primary-container`, `--color-on-surface-variant`, `--color-surface-container-highest`, and so on — never camelCased). The exhaustive key/value list is reproduced in `01-PATTERNS.md`; copy values verbatim. Include the `error` / `on-error` / `error-container` / `on-error-container` pair set even though the UI-SPEC notes no destructive action exists in v1 — success criterion #2 requires the full palette be present and usable, and these are retained for completeness only. Do not wire them to anything.

**Typography** — for each of the 7 roles (`display-lg`, `display-lg-mobile`, `headline-md`, `body-lg`, `body-md`, `mono-label`, `mono-code`) emit a `--font-{role}` family declaration plus a `--text-{role}` size, and then each modifier as its own separate custom property using the double-hyphen suffix form: `--text-{role}--line-height`, `--text-{role}--letter-spacing`, `--text-{role}--font-weight`. Emit `--text-{role}--letter-spacing` only for the four roles that actually declare `letterSpacing` in DESIGN.md (`display-lg`, `display-lg-mobile`, `headline-md`, `mono-label`); `body-lg`, `body-md`, and `mono-code` have no letter-spacing and must not get a fabricated one. Family mapping: `display-lg`, `display-lg-mobile`, `headline-md` are Lexend; `body-lg`, `body-md` are Inter; `mono-label`, `mono-code` are JetBrains Mono. Give each family declaration an appropriate generic fallback (`sans-serif` for Lexend/Inter, `monospace` for JetBrains Mono). The point of the double-hyphen form is that `text-headline-md` then applies size, line-height, letter-spacing, and weight together — a v3-style tuple would apply the size alone and leave the rest silently missing.

**Spacing** — `--spacing-unit: 8px`, `--spacing-gutter: 24px`, `--spacing-margin-mobile: 20px`, `--spacing-section-gap: 160px`, and `--spacing-container-max: 1280px`, all from DESIGN.md's `spacing:` block. Note that `container-max` is a max-width value that DESIGN.md happens to file under `spacing`; the UI-SPEC contracts the class name `max-w-container-max`, and in Tailwind v4 `max-w-*` resolves from the `--container-*` namespace rather than `--spacing-*`. So additionally emit `--container-container-max: 1280px` with a comment explaining it exists purely so the UI-SPEC-contracted `max-w-container-max` utility resolves. Tailwind's default 4px-increment spacing scale must remain available — these five named tokens are additive, not a replacement.

**Radius** — `--radius-sm: 0.125rem`, `--radius-md: 0.375rem`, `--radius-lg: 0.5rem`, `--radius-xl: 0.75rem`, `--radius-full: 9999px`, taking DESIGN.md's `rounded:` block as canonical over `code.html`'s divergent one. For DESIGN.md's `DEFAULT: 0.25rem` key, the bare `rounded` utility in Tailwind v4 reads the unsuffixed `--radius` custom property — emit `--radius: 0.25rem`. RESEARCH.md Pattern 1 speculatively suggested `--radius-DEFAULT`; that spelling is unverified, so confirm empirically which one makes the bare `rounded` class compile to `border-radius: 0.25rem` in the built stylesheet and keep whichever works. This is an explicit acceptance criterion below, not an assumption to carry forward.

Then extend `src/pages/index.astro`'s scaffolding `<main>` into a token gallery. Tailwind v4 emits all `@theme` custom properties into the output stylesheet regardless of use, but it only emits *utility class rules* for classes it actually sees — so the gallery must use at least one representative utility per token family for the build-output assertions to mean anything. Include, at minimum: `bg-surface-container`, `text-on-surface-variant`, `border-outline-variant`, `text-display-lg`, `text-headline-md`, `text-body-md`, `text-mono-label`, `font-mono-label`, `px-margin-mobile`, `gap-gutter`, `max-w-container-max`, `rounded`, `rounded-lg`, and `rounded-full`. Keep the gallery flat and unstyled beyond those classes — plain `<div>`/`<span>`/`<p>` elements with short literal labels. It is not a design comp; it is an assertion surface. Preserve the existing `PIPELINE OK` heading, the seven `<Icon />` elements, and the scaffolding comment from plan 01-01.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run verify:sec01 &amp;&amp; node -e "const fs=require('fs'),p=require('path');const d=p.join('dist');const css=[];(function w(x){for(const e of fs.readdirSync(x,{withFileTypes:true})){const f=p.join(x,e.name);if(e.isDirectory())w(f);else if(f.endsWith('.css'))css.push(fs.readFileSync(f,'utf8'))}})(d);const all=css.join('\n');const need=['--color-primary-container','--color-on-surface-variant','--color-surface-container-highest','--text-display-lg','--text-headline-md','--spacing-margin-mobile','--spacing-section-gap','--radius-full'];for(const n of need)if(!all.includes(n))throw new Error('missing '+n);console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 and `npm run verify:sec01` still exits 0 with `external_refs=0`.
    - The built stylesheet under `dist/` contains all 47 `--color-*` custom properties, one per DESIGN.md `colors:` key, and `--color-primary-container` resolves to `#00f0ff`, `--color-surface` to `#111417`, `--color-on-surface-variant` to `#b9cacb`.
    - The built stylesheet contains `--text-display-lg` at `72px` together with `--text-display-lg--line-height` `80px`, `--text-display-lg--letter-spacing` `-0.02em`, and `--text-display-lg--font-weight` `700`.
    - A rule for `.text-headline-md` exists in the built stylesheet and sets `font-size`, `line-height`, `letter-spacing`, and `font-weight` — not `font-size` alone. Same for `.text-mono-label`.
    - No `--text-body-md--letter-spacing`, `--text-body-lg--letter-spacing`, or `--text-mono-code--letter-spacing` property exists — DESIGN.md declares none for those three roles.
    - The bare `.rounded` utility compiles to `border-radius: 0.25rem` in the built stylesheet, and `.rounded-full` compiles to `9999px` (not `code.html`'s erroneous `0.75rem`).
    - `.max-w-container-max` compiles to `max-width: 1280px`, and `.px-margin-mobile` to a 20px horizontal padding.
    - `src/styles/global.css` contains exactly one `@theme` block and zero occurrences of `@import url(`.
    - No file matching `tailwind.config.*` exists in the repository.
    - `npx astro check` exits 0 with 0 errors.
  </acceptance_criteria>

  <done>Every colour, type role, spacing value, and radius from DESIGN.md is present in `@theme` and generates working Tailwind utilities carrying the exact source values, including the multi-property type roles.</done>
</task>

<task type="auto">
  <name>Task 2: Port the prototype's custom CSS primitives and automate DESIGN.md fidelity</name>

  <files>src/styles/global.css, scripts/verify-design-tokens.mjs</files>

  <read_first>
    - `Arquivos de design/code.html` lines 13-69 — the `<style>` block holding the six custom classes to port, with their exact gradient stops, blur radii, rgba values, and the `max-width: 768px` grid media query
    - `Arquivos de design/DESIGN.md` lines 106-164 — the "Elevation & Depth", "Layout & Spacing", and "Components" prose that these classes implement
    - `src/styles/global.css` — current state after Task 1
    - `src/layouts/Base.astro` — the element that already carries `bg-grid-pattern`, `glow-cloud-top-right`, and `glow-cloud-bottom-left` class names from plan 01-01, which this task makes real
    - `scripts/verify-no-external-origins.mjs` — the existing gate, as the structural model for a dependency-free Node ESM verification script
    - `scripts/verify-design-tokens.mjs` — the current failing stub from plan 01-01
  </read_first>

  <action>
Append plain CSS (outside the `@theme` block) to `src/styles/global.css`, porting the six primitives from `code.html` lines 13-69 verbatim in behaviour. These cannot be expressed as Tailwind utilities alone, which is why they are classes rather than tokens:

`.glow-cloud-top-right` and `.glow-cloud-bottom-left` — absolutely positioned, non-interactive (`pointer-events: none`), `z-index: 0`, 60vw square, offset `-20vh` vertically and `-10vw` horizontally toward their named corner, filled with a radial gradient from cyan at 0.05 alpha (top-right) or 0.03 alpha (bottom-left) fading to transparent `#111417` at 70%.

`.bg-grid-pattern` — a 64px square grid drawn with two 1px linear gradients (horizontal and vertical) in `rgba(185, 202, 203, 0.03)`, dropping to a 32px grid inside a `max-width: 768px` media query. That mobile reduction is LAY-01's "background grid reduzido" and must be preserved.

`.glass-panel` — `rgba(20, 27, 34, 0.6)` fill, 16px backdrop blur with the `-webkit-` prefixed twin for Safari, and a 1px `rgba(255, 255, 255, 0.1)` border.

`.border-glow-cyan` — 1px solid `#00F0FF` border with an `0 0 8px rgba(0, 240, 255, 0.4)` box-shadow.

`.btn-primary:hover` — `0 0 16px rgba(0, 240, 255, 0.6)` glow and a background of `#7df4ff` (the `primary-fixed` token).

Plus the prototype's body reset (`margin: 0; padding: 0; overflow-x: hidden;`) and the webkit scrollbar styling (8px wide, `#111417` track, `#323539` thumb with 4px radius, `#849495` on hover).

Where a value corresponds to a token already defined in `@theme`, reference the custom property rather than repeating the literal hex — for example the cyan in `.border-glow-cyan` is `--color-primary-container` and the scrollbar track is `--color-surface`. Keep the rgba composites as literals where no token expresses them (the glass fill and the grid line colour are alpha composites with no token equivalent).

Then replace the `scripts/verify-design-tokens.mjs` stub with the real fidelity gate — a dependency-free Node ESM script that treats `Arquivos de design/DESIGN.md` as the source of truth rather than hardcoding a second copy of the values, so that any future DESIGN.md edit is caught rather than silently diverging. Contract:
- Read `Arquivos de design/DESIGN.md`, extract the YAML frontmatter between the leading `---` fences, and parse its four blocks (`colors`, `typography`, `rounded`, `spacing`) with a small line-based reader. The structure is fixed and regular — two-space-indented `key: value` pairs under `colors`/`rounded`/`spacing`, and two-space-indented role names with four-space-indented `fontFamily`/`fontSize`/`fontWeight`/`lineHeight`/`letterSpacing` children under `typography`. Strip surrounding single quotes from values. Do not add a YAML dependency.
- Read `src/styles/global.css` and isolate the `@theme { … }` block. For every parsed colour key assert the block declares `--color-{key}` with a case-insensitively matching value. For every typography role assert `--font-{role}`, `--text-{role}`, `--text-{role}--line-height`, and `--text-{role}--font-weight` are present with matching values, and assert `--text-{role}--letter-spacing` is present if and only if DESIGN.md declares `letterSpacing` for that role. For every `rounded` key assert `--radius-{key}` is present, mapping the `DEFAULT` key to whichever unsuffixed spelling Task 1 established. For every `spacing` key assert `--spacing-{key}`.
- Then read the built CSS under `dist/` (recursively, all `.css` files) and assert every `--color-*` custom property survived into the output, and that rules exist for the representative utilities the token gallery uses. Exit 1 with the message `dist/ not found — run npm run build first` if `dist/` is absent.
- Additionally assert `src/styles/global.css` defines all six custom class selectors (`.glow-cloud-top-right`, `.glow-cloud-bottom-left`, `.bg-grid-pattern`, `.glass-panel`, `.border-glow-cyan`, `.btn-primary:hover`) and that a `max-width: 768px` media query re-declares the grid background size.
- Print every mismatch with the token name, the DESIGN.md value, and the value actually found. Print a final summary line `TOKENS SUMMARY colors=<n>/<n> typography=<n>/<n> radius=<n>/<n> spacing=<n>/<n> custom_classes=<n>/6 mismatches=<n>`. Exit 0 only when `mismatches=0`.
- Because the script derives expectations by parsing the source document, it must fail loudly if the parse yields an implausible count — abort with a clear error if fewer than 40 colour keys, fewer than 7 typography roles, fewer than 6 radius keys, or fewer than 5 spacing keys are extracted, so a broken parser can never produce a vacuous pass.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run verify:tokens &amp;&amp; npm run verify:sec01</automated>
  </verify>

  <acceptance_criteria>
    - `npm run verify:tokens` exits 0 and prints a `TOKENS SUMMARY` line with `mismatches=0`, `custom_classes=6/6`, and colour/typography/radius/spacing counts where the numerator equals the denominator.
    - The parsed counts in that summary are at least 47 colours, 7 typography roles, 6 radius keys, and 5 spacing keys — proving the DESIGN.md parser actually read the document rather than passing vacuously.
    - Deliberately mutating one hex value in `src/styles/global.css` makes `npm run verify:tokens` exit 1 and name that specific token in its output; restoring the value makes it exit 0 again. (The gate detects drift, it does not just check for presence.)
    - `src/styles/global.css` contains all six selectors: `.glow-cloud-top-right`, `.glow-cloud-bottom-left`, `.bg-grid-pattern`, `.glass-panel`, `.border-glow-cyan`, `.btn-primary:hover`.
    - `src/styles/global.css` contains a `@media (max-width: 768px)` rule that sets `background-size: 32px 32px` for `.bg-grid-pattern`.
    - `.glass-panel` declares both `backdrop-filter` and `-webkit-backdrop-filter` at `blur(16px)`.
    - The built CSS under `dist/` contains rules for `.glass-panel` and `.bg-grid-pattern`.
    - `npm run verify:sec01` still exits 0 with `external_refs=0` — no external origin was introduced by the CSS additions.
    - `npx astro check` exits 0 with 0 errors.
  </acceptance_criteria>

  <done>The prototype's glassmorphism, background grid (with its mobile reduction), atmospheric glows, and cyan glow stroke are available as real CSS, and DESIGN.md-to-`@theme` fidelity is enforced by a gate that re-derives its expectations from DESIGN.md on every run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design prototype (`code.html`) → production stylesheet | The prototype's `<style>` block is being ported. Its sibling `<head>` tags carry external origins, so copy-paste from this file is the concrete SEC-01 risk. |
| `DESIGN.md` → compiled CSS asset | Token values flow one-way from a repo-authored document into a static CSS file served to every visitor. There is no runtime input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | `src/styles/global.css` acquiring an `@import url("https://…")` while porting the prototype's `<style>` block — CSS `@import` is a fully-fledged external origin and one of the three SEC-01 surfaces named in `01-PATTERNS.md` | mitigate | Only the `<style>` block content (lines 13-69) is ported; the prototype's external `<link>`/`<script>` tags on lines 7-12 are out of scope for this plan entirely. `npm run verify:sec01` is re-run as an acceptance criterion on both tasks and matches CSS `@import` with absolute URLs. |
| T-01-08 | Tampering (data integrity) | Silent token drift between `DESIGN.md` and `@theme` — 47 hex values plus 7 multi-property type roles cannot be verified by human inspection, and a wrong value propagates invisibly into every component built in Phases 2-5 | mitigate | `scripts/verify-design-tokens.mjs` re-parses `DESIGN.md` on every run and diffs it against both the source `@theme` block and the built CSS. The gate is required to abort on implausible parse counts so it cannot pass vacuously, and its drift-detection behaviour is itself proven by a mutate-and-restore acceptance criterion. |
| T-01-09 | Tampering | Copying `code.html`'s divergent `borderRadius` block, whose `full: 0.75rem` is a prototype authoring error that would break every pill/circular element built in Phase 3 | mitigate | The `<source_of_truth>` section states the divergence explicitly and names DESIGN.md as canonical; an acceptance criterion asserts `.rounded-full` compiles to `9999px`. |
| T-01-10 | Denial of Service | Unbounded CSS growth — `@theme` emits every declared custom property into the stylesheet on every page load | accept | 47 colours plus roughly 40 other properties is a few kilobytes of gzipped text on a static CDN-served asset. Trivial cost, and success criterion #2 explicitly requires the full palette be present and usable by any component. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a — no packages are installed by this plan | This plan adds zero dependencies. The Package Legitimacy Gate was satisfied in plan 01-01 (RESEARCH.md audit: 10 packages, all `[OK]`, zero `[SUS]`/`[SLOP]`). |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npm run verify:tokens` exits 0 with `mismatches=0` and `custom_classes=6/6`.
- `npm run verify:sec01` exits 0 with `external_refs=0`.
- `npx astro check` exits 0 with 0 errors.
- No `tailwind.config.*` file exists in the repository.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion #2 is met: every DESIGN.md token is wired into Tailwind's `@theme` and usable by any component, proven by utilities compiling to the exact source values in the built stylesheet. The prototype's four visual primitives render. Token fidelity is guarded by an automated gate that fails on drift rather than on absence alone.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md` when done.
</output>
