{
  "name": "portifolio-pessoal",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify:sec01": "node scripts/verify-no-external-origins.mjs",
    "verify:tokens": "node scripts/verify-design-tokens.mjs",
    "verify:schema": "node scripts/verify-content-schema.mjs",
    "verify": "npm run build && npm run verify:sec01 && npm run verify:tokens && npm run verify:schema"
  },
  "dependencies": {
    "astro": "^7.2.10",
    "tailwindcss": "^4.3.3",
    "@tailwindcss/vite": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
